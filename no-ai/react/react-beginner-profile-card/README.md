    # Beginner Profile Card

    Students edit components to create a personal profile page.

    ## Project Goal
    This project is already set up for students. They do **not** need to build the project from scratch.
    Their job is to open the React components and edit the JSX content.

    ## Student Tasks
    1. Update the ProfileCard component with your own sample details.
2. Add 3 hobbies in the HobbyList component.
3. Edit the Header component text.
4. Edit the Footer component with a practice message.
5. Change one or two styles in App.css.

    ## Project Structure
    - `src/App.jsx` - main page layout
    - `src/App.css` - styles
    - `src/components/Header.jsx` - top section
    - `src/components/Footer.jsx` - bottom section
    - one or more feature components depending on the project

    ## How to Run
    1. Open a terminal in this project folder
    2. Run `npm install`
    3. Run `npm run dev`
    4. Open the local browser URL shown by Vite

    ## Student Focus
    - Edit component text
    - Add JSX elements
    - Update arrays and lists
    - Practice props and reusable components
    - Make small style changes
    